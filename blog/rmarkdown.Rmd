rmarkdown test with a plot
==================

<!-- rss=FALSE -->

This is a test for a post in **rmarkdown** format.

```{r}
hist(rnorm(100))
```

$a = 1$

## Session Info

```{r}
sessionInfo()
```

<!-- uid=rmarkdown -->
